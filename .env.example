# Copy this file to .env and fill values, or set the equivalent GitHub repository secrets:
# - SPOTIFY_CLIENT_ID
# - SPOTIFY_CLIENT_SECRET
# - SPOTIFY_REFRESH_TOKEN
#
# The refresh token must be obtained from a prior OAuth authorization flow that included:
#   user-read-private user-read-email playlist-modify-public playlist-modify-private
# (or at least the playlist-modify scopes for the playlists you intend to edit).
#
# CSV_PATH can be a path inside the repository (relative) or an absolute path on disk. Default: tracks.csv

SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret
SPOTIFY_REFRESH_TOKEN=your_spotify_oauth_refresh_token

# Path to the CSV file to read (optional; workflow can pass via input)
CSV_PATH=tracks.csv

# Playlist IDs (defaults are set in the script; override if you want different playlists)
PLAYLIST_ONE_ID=5TGXCfKbeG0emEZjm6hMRJ
PLAYLIST_TWO_ID=5vQfOSbBgybQkWSSrILyr9
